# ADB (recovery runs unauthenticated)
ro.adb.secure=0

# Keystore / Keymaster (FBEv2 wrappedkey metadata, SPU keymint)
fbe.metadata.wrappedkey=true

# FUSE passthrough
persist.sys.fuse.passthrough.enable=true
